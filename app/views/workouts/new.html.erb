<h1 class='display-3'>Create Workout</h1>

<%= form_for @workout do |f| %>
  <%= f.label :name, :class => 'form-label'%>
  <%= f.text_field :name, :required => true, :class => 'form-control' %>

  <%= f.submit nil, :class => 'mt-3 btn btn-primary' %>
<% end %>
